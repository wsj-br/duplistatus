# Versión 1.3.0 {#version-130}

## Resumen {#overview}

Versión 1.3.0 introduce políticas de contraseña configurables, un nuevo Visor de Logs de aplicación para administradores, visualización de días de la semana consciente de la configuración regional, y varias mejoras en la experiencia del usuario. Esta versión se enfoca en herramientas administrativas mejoradas y soporte de internacionalización mejorado.

## Nuevas características {#new-features}

### Política de contraseña configurable {#configurable-password-policy}

- **Variables de entorno de política de contraseña**: Configure los requisitos de validación de contraseña a través de variables de entorno:
  - `PWD_ENFORCE`: Establezca en `false` para desactivar los requisitos de complejidad de contraseña (mayúsculas, minúsculas, números). Por defecto, se aplican todas las reglas. Cuando está deshabilitado, solo se aplica la longitud mínima.
  - `PWD_MIN_LEN`: Establece la longitud mínima de contraseña en caracteres. Por defecto 8 caracteres.
  - La verificación de contraseña por defecto (evitando el uso de la contraseña de administrador por defecto) siempre se aplica independientemente de la configuración `PWD_ENFORCE`.

### Visor de Logs de aplicación {#application-logs-viewer}

- **Nuevo visor de logs solo para administradores**: Ver y descargar Logs de aplicación directamente desde la página de Configuración:
  - Acceda al archivo consolidado `application.log` y versiones de logs rotados
  - Selector de conteo de líneas configurable (100, 500, 1000, 5000, 10000 líneas)
  - Filtro de búsqueda en tiempo real con coincidencia de texto sin distinción de mayúsculas y minúsculas
  - Alternancia de desplazamiento automático para monitoreo de logs en tiempo real con sondeo automático
  - Copiar al portapapeles y exportar logs filtrados como archivo de texto
  - Visualización de metadatos de archivo mostrando conteo de líneas, tamaño del archivo y marca de tiempo de última modificación
  - Rotación de logs al iniciar el contenedor (configurable a través de `LOG_ROTATION_VERSIONS`, por defecto: 5)

### Guía de usuario consciente del contexto {#context-aware-user-guide}

- **Botón de ayuda dinámico**: El botón de Guía de usuario en el encabezado de la aplicación ahora abre documentación relevante para la página actual. La información sobre herramientas se actualiza para mostrar "Ayuda para {pageName}" para indicar qué documentación se abrirá.

### Mejora de configuración del servidor {#server-configuration-enhancement}

- **Acceso rápido a la interfaz de usuario de Duplicati**: Hacer clic derecho en el botón de configuración del servidor o en cualquier elemento del servidor en el popover de selección de servidor abre la interfaz de usuario heredada de Duplicati (`/ngax`) en una nueva pestaña.

---

## 🔧 Mejoras {#improvements}

### Visualización de días de la semana consciente de la configuración regional {#locale-aware-weekday-display}

- **Ordenamiento de días de la semana internacionalizado**: La visualización de días de la semana en el formulario de monitoreo de backups retrasados ahora respeta la configuración regional de su navegador. Los días de la semana se muestran en el orden correcto según la configuración regional (lunes-domingo para Reino Unido/Europa, domingo-sábado para EE.UU./Brasil).

### Manejo mejorado de errores SMTP {#enhanced-smtp-error-handling}

- **Mensajes de error mejorados**: Los mensajes de error SMTP ahora proporcionan orientación más precisa:
  - Los errores de autenticación indican claramente problemas de nombre de usuario/contraseña
  - Los errores de versión SSL/TLS se identifican correctamente
  - Las sugerencias de tipo de conexión solo aparecen cuando son genuinamente relevantes

### Comportamiento de notificación de brindis {#toast-notification-behaviour}

- **Notificaciones de error persistentes**: Los brindis de error ahora persisten hasta que se cierren manualmente, asegurando que no pierda mensajes de error importantes. Los brindis de éxito y informativos se descartan automáticamente después de 3 segundos.

### Tabla de configuración de notificaciones {#notification-settings-table}

- **Diseño simplificado**: La tabla de configuración de notificaciones se ha simplificado con una columna combinada "Servidor / Backup" e iconos contextuales que indican el estado de configuración de destino adicional.

---

## 🚀 Notas de migración {#migration-notes}

### De la versión 1.2.x {#from-version-12x}

Al actualizar a la versión 1.3.0:

1. **Política de contraseña**: La nueva política de contraseña por defecto aplica todos los requisitos de complejidad. Si necesita una política de contraseña más simple, configure las variables de entorno `PWD_ENFORCE` y `PWD_MIN_LEN`.

2. **Logs de aplicación**: Toda la salida del contenedor (servidor y servicio cron) ahora se consolida en un único archivo `data/logs/application.log`.

3. **Rotación de logs**: La rotación de logs es configurable a través de la variable de entorno `LOG_ROTATION_VERSIONS` (por defecto: 5 versiones).

4. **No se requiere migración de base de datos**: Esta versión no requiere cambios en el esquema de la base de datos.

---

## Puntos finales de API {#api-endpoints}

El siguiente punto final de API se ha introducido en esta versión:

### Punto final de política de contraseña {#password-policy-endpoint}

- `GET /api/auth/password-policy` - Devuelve la configuración de política de contraseña actual

### Puntos finales de Logs de aplicación {#application-logs-endpoints}

- `GET /api/application-logs` - Ver logs con selección de archivo y funcionalidad de cola
- `GET /api/application-logs/export` - Exportar logs filtrados como archivo de texto

### Puntos finales eliminados {#removed-endpoints}

Los siguientes puntos finales de API no utilizados se han eliminado en esta versión:

- `DELETE /api/backups/[backupId]` - Punto final solo para desarrollo que nunca se utilizó
- `GET /api/audit-log/stats` - El punto final no fue llamado por el visor de log de auditoría

---

## Soporte {#support}

### Obtener ayuda {#getting-help}

- **Documentación**: [Guía de usuario](../user-guide/overview.md)
- **Configuración de correo electrónico**: [Guía de configuración de correo electrónico](../user-guide/settings/email-settings.md)
- **Referencia de API**: [Documentación de API](../api-reference/overview.md)
- **Guía de migración**: [Migración de actualización de versión](../migration/version_upgrade.md)
- **Comunidad**: [Discusiones de GitHub](https://github.com/wsj-br/duplistatus/discussions)
- **Problemas**: [Problemas de GitHub](https://github.com/wsj-br/duplistatus/issues)

### Reportar errores {#reporting-bugs}

Al reportar errores, incluya:

- Versión: 1.3.0
- Sistema operativo y versión
- Versión de Docker/podman
- Tipo de contenedor (Docker o podman/Pod)
- Mensajes de error y Logs
- Pasos para reproducir

---

## Registro de cambios {#changelog}

### Cambios de la versión 1.3.0 {#version-130-changes}

- **Añadido**: Política de contraseña configurable a través de variables de entorno `PWD_ENFORCE` y `PWD_MIN_LEN`
- **Añadido**: Visor de Logs de aplicación en Configuración (solo administrador)
- **Añadido**: Rotación de logs con conteo de versión configurable (`LOG_ROTATION_VERSIONS`)
- **Añadido**: Botón de Guía de usuario consciente del contexto que abre documentación relevante
- **Añadido**: Clic derecho en configuración del servidor para abrir la interfaz de usuario heredada de Duplicati
- **Añadido**: Punto final de API `GET /api/auth/password-policy` para configuración de política de contraseña
- **Añadido**: Puntos finales de API para visualización y exportación de Logs de aplicación
- **Cambiado**: La visualización de días de la semana ahora respeta la configuración regional del navegador
- **Cambiado**: Los brindis de error persisten hasta que se cierren manualmente
- **Cambiado**: Los brindis de éxito se descartan automáticamente después de 3 segundos (anteriormente 5 segundos)
- **Cambiado**: Tabla de configuración de notificaciones simplificada con columna combinada "Servidor / Backup"
- **Cambiado**: Consolidado todos los logs del contenedor en un único archivo `application.log`
- **Mejorado**: Los mensajes de error SMTP proporcionan orientación más precisa
- **Eliminado**: Puntos finales de API no utilizados (`DELETE /api/backups/[backupId]` y `GET /api/audit-log/stats`) que no eran llamados por la aplicación

---

## Licencia {#license}

Este proyecto está licenciado bajo la [Licencia Apache 2.0](https://github.com/wsj-br/duplistatus/blob/main/LICENSE).

**Copyright © 2025 Waldemar Scudeller Jr.**
