---
translation_last_updated: '2026-05-11T14:27:44.859Z'
source_file_mtime: '2026-05-06T23:18:51.430Z'
source_file_hash: 76e99e418e73fdc1f98c589e1c65689d2e36edd0f55daf032c7688c9017a89fd
translation_language: de
source_file_path: documentation/docs/release-notes/1.0.x.md
translation_models:
  - anthropic/claude-3.5-haiku
  - anthropic/claude-haiku-4.5
  - 'nvidia/nemotron-nano-12b-v2-vl:free'
  - openai/gpt-4o-mini
  - qwen/qwen3-235b-a22b-2507
---
# Version 1.0.x {#version-10x}

## Übersicht {#overview}

Version 1.0.x ist eine Feature-Release, die bedeutende Verbesserungen der E-Mail-Konfiguration, der Serververwaltung und der Datenbankwartungs-Tools einführt. Diese Release konzentriert sich auf die Verbesserung der Zuverlässigkeit von E-Mail-Benachrichtigungen, die Erweiterung der SMTP-Verbindungsoptionen und die Bereitstellung besserer Tools zur Verwaltung von Servern und Datenbankintegrität.

## Neue Funktionen {#new-features}

### Serververwaltung – Verbesserungen {#server-management-enhancements}

- **Funktionalität zum Ändern des Serverpassworts** - Benutzer können jetzt Serverpasswörter direkt von der Serverdetailseite aus ändern, indem sie auf das Schlüsselsymbol klicken
- **Erkennung und Zusammenführung doppelter Server** - Neue Funktion im Formular der Datenbankwartung zur Erkennung doppelter Server und deren Zusammenführung in einen einzelnen Servereintrag

### Verbesserungen der Audit-Protokollierung {#audit-logging-enhancements}

- **Audit-Protokollierung für Sicherungshochladungen** - Alle Sicherungsprotokoll-Hochladungen über den `/api/upload`-Endpunkt werden jetzt automatisch im Audit-Log protokolliert. Jeder Eintrag enthält Sicherungsdetails (Server-ID, Servername, Sicherungsname, Status, Datum, Dauer, Dateizahlen, Größen, Warnungen und Fehler) zusammen mit IP-Adresse des Clients und User-Agent-Informationen für Nachverfolgung und Sicherheitszwecke

### E-Mail-Konfiguration – Verbesserungen {#email-configuration-enhancements}

- **Hinweis zur unvollständigen Konfiguration** - Visuelle Warnung, die angezeigt wird, wenn die E-Mail-Einstellungen nicht ordnungsgemäß konfiguriert sind
- **Statusanzeigen für den Dienst** - "(deaktiviert)"-Labels werden auf den Benachrichtigungs-Checkboxen angezeigt, wenn die E-Mail-Dienste nicht konfiguriert sind
- **Anpassbare Absenderinformationen**:
  - `Sender Name`-Feld zur Anpassung des angezeigten Namens, der als Absender in E-Mail-Benachrichtigungen angezeigt wird
  - `From Address`-Feld zur Angabe der E-Mail-Adresse, die als Absender angezeigt wird
  - Hinweis: Einige E-Mail-Anbieter überschreiben das `From Address` mit dem `SMTP Server Username`
- **Umschalter für SMTP-Authentifizierung** - Option zum Aktivieren oder Deaktivieren der SMTP-Authentifizierung, unterstützt Server, die keine Authentifizierung erfordern
- **Validierung des E-Mail-Formats** - Validierung für die Felder Empfänger- und Absenderadresse, um ein korrektes E-Mail-Format sicherzustellen
- **Erweiterte Test-E-Mail** - Die Test-E-Mail zeigt jetzt umfassende Verbindungsdetails an, einschließlich SMTP-Server-Hostname, Port, Verbindungstyp, Authentifizierungsstatus, Benutzername (falls zutreffend), Empfänger-E-Mail, Absenderadresse, Absendername und Test-Zeitstempel

### SMTP-Verbindungstyp-Unterstützung {#smtp-connection-type-support}

- **Mehrere Verbindungstypen** - Unterstützung für drei Verbindungstypen:
  - `Plain SMTP` (keine Verschlüsselung) auf Port 25
  - `STARTTLS` (Upgrade auf TLS) auf Port 587
  - `Direct SSL/TLS` (verschlüsselt von Anfang an) auf Port 465
- **Standardverbindungstyp** - Geändert auf `Direct SSL/TLS` für neue Konfigurationen
- **Verbindungsspezifische Anforderungen** - `From Address`-Feld ist erforderlich für `Plain SMTP`-Verbindungen, um eine ordnungsgemäße Identifizierung des E-Mail-Absenders und die Einhaltung von RFC 5322 sicherzustellen

---

## 🐛 Fehlerbehebungen {#-bug-fixes}

### E-Mail-System-Fixes {#email-system-fixes}

- **Fehler im fehlenden `From`-Header behoben** - RFC 5322-Konformitätsfehler behoben, indem sichergestellt wurde, dass alle E-Mail-Nachrichten ordnungsgemäße `From`-Header enthalten
- **Fehler bei der E-Mail-Verschlüsselung für STARTTLS behoben** - Korrektur der Verschlüsselungsbehandlung für `STARTTLS`-Verbindungen (Port 587)
- **Fehler bei einfachen SMTP-Verbindungen behoben** - TLS-Verhandlungsfehler behoben, die dazu führten, dass `Plain SMTP`-Verbindungen fehlschlugen
- **Fehler beim Verlust nicht gespeicherter Konfigurationen behoben** - Verhindert, dass nicht gespeicherte E-Mail-Konfigurationen gelöscht werden, wenn das Passwort geändert wird
- **Fehler bei veralteter SMTP-Konfiguration behoben** - Problem behoben, bei dem der Test-E-Mail-Endpunkt eine veraltete SMTP-Konfiguration verwendete

### Behobene Probleme in der Benutzeroberfläche {#user-interface-fixes}

- **Passwort-Sichtbarkkeitsumschalter behoben** - Korrigiertes Verhalten der Passwort-Anzeigefunktion im Passwortänderungsformular; durch Klicken auf das Augensymbol wird das Passwort jetzt für beide Passwortfelder gleichzeitig angezeigt

### Systembehebungen {#system-fixes}

- **Docker-Entrypoint-Protokollierung behoben** - Problem behoben, bei dem das Docker-Entrypoint-Skript Nachrichten verwarf
- **Berechnung des Nächsten Laufs behoben** - Automatische Neuberechnung des `Next Run`-Datums nach dem Erfassen von Sicherungsprotokollen hinzugefügt, um sicherzustellen, dass das Datum nach der Protokollerfassung korrekt aktualisiert wird

---

## 🚀 Hinweise zur Migration {#-migration-notes}

### Aus Version 0.9.x {#from-version-09x}

Dieses Release enthält Verbesserungen der E-Mail-Konfiguration und Serververwaltung. Beim Upgrade von Version 0.9.x:

1. **Keine Datenbankmigration erforderlich** - Dieses Release erfordert keine Änderungen am Datenbankschema
2. **Aktualisierungen der E-Mail-Konfiguration** - Bestehende E-Mail-Konfigurationen funktionieren weiterhin, aber Sie sollten Ihre Einstellungen überprüfen und aktualisieren, um die neuen Funktionen zu nutzen:
   - Ziehen Sie in Betracht, eine benutzerdefinierte `Sender Name` für eine bessere E-Mail-Identifikation festzulegen
   - Überprüfen Sie, ob Ihr `From Address` korrekt eingestellt ist, insbesondere wenn Sie `Plain SMTP`-Verbindungen verwenden
   - Testen Sie Ihre E-Mail-Konfiguration mit der erweiterten Test-E-Mail-Funktion
3. **Standardverbindungstyp** - Neue E-Mail-Konfigurationen werden standardmäßig auf den Verbindungstyp `Direct SSL/TLS` eingestellt
4. **Server-Passwortverwaltung** - Sie können jetzt Server-Passwörter direkt von der Serverdetailseite ändern, ohne Backups erneut sammeln zu müssen

### E-Mail-Konfiguration – Empfehlungen {#email-configuration-recommendations}

- **Für die Produktionsnutzung**: Verwenden Sie `STARTTLS`- oder `Direct SSL/TLS`-Verbindungen für sichere E-Mail-Zustellung
- **Für lokale Tests**: `Plain SMTP`-Verbindungen sind für vertrauenswürdige lokale Netzwerke verfügbar
- **Absenderadresse erforderlich**: Wann Sie `Plain SMTP` verwenden oder Wann die Authentifizierung deaktiviert ist, stellen Sie sicher, dass das Feld `Absenderadresse` ordnungsgemäß konfiguriert ist
- **Testen Sie Ihre Konfiguration**: Verwenden Sie immer die Schaltfläche „Test-E-Mail senden", um zu bestätigen, dass Ihre E-Mail-Einrichtung ordnungsgemäß funktioniert

---

## API-Endpunkte {#api-endpoints}

### E-Mail-Konfiguration Endpoints {#email-configuration-endpoints}

- `GET /api/email/config` - E-Mail-Konfiguration abrufen
- `POST /api/email/config` - E-Mail-Konfiguration aktualisieren
- `POST /api/email/test` - Test-E-Mail senden
- `DELETE /api/email/config` - E-Mail-Konfiguration löschen

### Serververwaltungs-Endpunkte {#server-management-endpoints}

- `PATCH /api/servers/{id}/password` - Server-Passwort aktualisieren
- `POST /api/database/maintenance/merge-duplicate-servers` - Doppelte Server zusammenführen

---

## Unterstützung {#support}

### Hilfe {#getting-help}

- **Dokumentation**: [Benutzerhandbuch](../user-guide/overview.md)
- **E-Mail-Einstellungen**: [Anleitung zur E-Mail-Konfiguration](../user-guide/settings/email-settings.md)
- **API-Referenz**: [API-Dokumentation](../api-reference/overview.md)
- **Migrationsanleitung**: [Migration bei Versionsupgrade](../migration/version_upgrade.md)
- **Community**: [GitHub-Diskussionen](https://github.com/wsj-br/duplistatus/discussions)
- **Probleme melden**: [GitHub Issues](https://github.com/wsj-br/duplistatus/issues)

### Fehler melden {#reporting-bugs}

Bei der Meldung von Fehlern bitte folgende Angaben machen:
- Version: 1.0.x (genaue Version angeben, z. B. 1.0.3)
- Betriebssystem und Version
- Docker/podman-Version
- Containertyp (Docker oder podman/Pod)
- Fehlermeldungen und Logs
- Schritte zur Reproduktion
- E-Mail-Konfigurationsdetails (falls e-mailbezogen)
- Podman-Konfigurationsdetails (falls in podman/Pod ausgeführt)

---

## Änderungsprotokoll {#changelog}

### Detaillierte Änderungen {#detailed-changes}

- **Hinzugefügt**: Funktion zum Ändern des Serverpassworts auf der Server-Details-Seite
- **Hinzugefügt**: Erkennung und Zusammenführung doppelter Server bei der Datenbankwartung
- **Hinzugefügt**: Felder `Sender Name` und `From Address` zur E-Mail-Konfiguration
- **Hinzugefügt**: Unterstützung für `Plain SMTP`-Verbindungen (Port 25)
- **Hinzugefügt**: Auswahl des Verbindungstyps (`Plain SMTP`, `STARTTLS`, `Direct SSL/TLS`)
- **Hinzugefügt**: Umschalter für SMTP-Authentifizierungspflicht
- **Hinzugefügt**: Formatüberprüfung von E-Mail-Adressen für Empfänger- und Absenderfelder
- **Hinzugefügt**: Verbesserter Test-E-Mail-Versand mit umfassenden Verbindungsdaten
- **Hinzugefügt**: Hinweis auf unvollständige Konfiguration bei E-Mail-Einstellungen
- **Hinzugefügt**: Label „(deaktiviert)“ bei Benachrichtigungs-Checkboxes, wenn Dienste nicht konfiguriert sind
- **Hinzugefügt**: Einträge im Audit-Log beim Hochladen von Backup-Logs über den `/api/upload`-Endpunkt
- **Geändert**: Standard-Verbindungstyp für neue Konfigurationen ist nun `Direct SSL/TLS`
- **Geändert**: Das Feld `From Address` ist nun für `Plain SMTP`-Verbindungen erforderlich
- **Behoben**: Fehlender `From`-Header in E-Mail-Nachrichten (RFC 5322-Konformität)
- **Behoben**: E-Mail-Verschlüsselung für `STARTTLS`-Verbindungen
- **Behoben**: `Plain SMTP`-Verbindungen schlagen mit TLS-Verhandlungsfehlern fehl
- **Behoben**: Nicht gespeicherte E-Mail-Konfiguration wurde beim Ändern des Passworts gelöscht
- **Behoben**: Veraltete SMTP-Konfiguration im Test-E-Mail-Endpunkt
- **Behoben**: Passwort-Sichtbarkeits-Umschalter im Passwortänderungsformular
- **Behoben**: Docker-Entrypoint-Skript verwirft Protokollmeldungen
- **Behoben**: `Next Run`-Datum wird nach dem Sammeln von Backup-Logs nicht neu berechnet
- **Verbessert**: Benutzerfreundlichkeit der E-Mail-Konfiguration durch bessere Validierung und Rückmeldung

---

## Lizenz {#license}

Dieses Projekt ist unter der [Apache License 2.0](https://github.com/wsj-br/duplistatus/blob/main/LICENSE) lizenziert.

**Copyright © 2025 Waldemar Scudeller Jr.**
