# Version 1.3.0

## Übersicht

Version 1.3.0 führt konfigurierbare Passwortrichtlinien, einen neuen Anwendungsprotokoll-Viewer für Administratoren, gebietsschemabewusste Wochentagsanzeige und mehrere Verbesserungen der Benutzerfreundlichkeit ein. Diese Version konzentriert sich auf verbesserte Verwaltungstools und verbesserte Unterstützung für Internationalisierung.

## Neue Funktionen

### Konfigurierbare Passwortrichtlinie

- **Passwortrichtlinien-Umgebungsvariablen**: Konfigurieren Sie Passwortvalidierungsanforderungen über Umgebungsvariablen:
  - `PWD_ENFORCE`: Setzen Sie auf `false`, um Passwort-Komplexitätsanforderungen (Großbuchstaben, Kleinbuchstaben, Zahlen) zu deaktivieren. Standardmäßig werden alle Regeln erzwungen. Wenn deaktiviert, wird nur die Mindestlänge erzwungen.
  - `PWD_MIN_LEN`: Legt die Mindestpasswortlänge in Zeichen fest. Standardmäßig 8 Zeichen.
  - Die Standard-Passwortprüfung (Verhinderung der Verwendung des Standard-Admin-Passworts) wird immer erzwungen, unabhängig von der `PWD_ENFORCE`-Einstellung.

### Anwendungsprotokoll-Viewer

- **Neuer Admin-only-Protokoll-Viewer**: Zeigen Sie Anwendungsprotokolle direkt von der Seite "Einstellungen" an und laden Sie sie herunter:
  - Greifen Sie auf die konsolidierte `application.log`-Datei und rotierte Protokollversionen zu
  - Konfigurierbarer Zeilenzähler-Selektor (100, 500, 1000, 5000, 10000 Zeilen)
  - Echtzeit-Suchfilter mit Groß-/Kleinschreibung-unabhängiger Textabstimmung
  - Automatisches Scrollen-Umschalter für Echtzeit-Protokollüberwachung mit automatischem Abrufen
  - In die Zwischenablage kopieren und gefilterte Protokolle als Textdatei exportieren
  - Dateimetadaten-Anzeige mit Zeilenanzahl, Dateigröße und letztem Änderungszeitstempel
  - Protokollrotation beim Container-Start (konfigurierbar über `LOG_ROTATION_VERSIONS`, Standard: 5)

### Kontextabhängiger Benutzerhandbuch

- **Dynamische Hilfeschaltfläche**: Die Schaltfläche "Benutzerhandbuch" in der App-Kopfzeile öffnet nun Dokumentation, die für die aktuelle Seite relevant ist. Der Tooltip wird aktualisiert, um "Hilfe für {pageName}" anzuzeigen, um anzugeben, welche Dokumentation geöffnet wird.

### Serverkonfigurationsverbesserung

- **Schnellzugriff auf Duplicati-Benutzeroberfläche**: Wenn Sie mit der rechten Maustaste auf die Serverkonfigurationsschaltfläche oder ein beliebiges Serverelement im Serverauswahlpopover klicken, wird die Duplicati-Legacy-Benutzeroberfläche (`/ngax`) in einem neuen Tab geöffnet.

---

## 🔧 Verbesserungen

### Gebietsschemabewusste Wochentagsanzeige

- **Internationalisierte Wochentagsreihenfolge**: Die Wochentagsanzeige im Überwachungsformular für überfällige Sicherungen respektiert nun die Gebietsschemaeinstellungen Ihres Browsers. Wochentage werden in der richtigen Reihenfolge basierend auf dem Gebietsschema angezeigt (Montag-Sonntag für Großbritannien/Europa, Sonntag-Samstag für USA/Brasilien).

### Verbesserte SMTP-Fehlerbehandlung

- **Verbesserte Fehlermeldungen**: SMTP-Fehlermeldungen bieten nun genauere Anleitung:
  - Authentifizierungsfehler zeigen deutlich Benutzername-/Passwortprobleme an
  - SSL/TLS-Versionsfehler werden korrekt identifiziert
  - Verbindungstypvorschläge werden nur angezeigt, wenn sie wirklich relevant sind

### Toast-Benachrichtigungsverhalten

- **Persistente Fehlerbenachrichtigungen**: Fehler-Toasts bleiben nun bestehen, bis sie manuell geschlossen werden, um sicherzustellen, dass Sie wichtige Fehlermeldungen nicht verpassen. Erfolgs- und Informations-Toasts werden nach 3 Sekunden automatisch geschlossen.

### Benachrichtigungseinstellungstabelle

- **Vereinfachtes Layout**: Die Benachrichtigungseinstellungstabelle wurde mit einer kombinierten Spalte "Server / Sicherung" und kontextuellen Symbolen, die den Konfigurationsstatus des zusätzlichen Ziels anzeigen, optimiert.

---

## 🚀 Migrationsnoten

### Von Version 1.2.x

Beim Upgrade auf Version 1.3.0:

1. **Passwortrichtlinie**: Die neue Passwortrichtlinie erzwingt standardmäßig alle Komplexitätsanforderungen. Wenn Sie eine einfachere Passwortrichtlinie benötigen, konfigurieren Sie die Umgebungsvariablen `PWD_ENFORCE` und `PWD_MIN_LEN`.

2. **Anwendungsprotokolle**: Alle Container-Ausgaben (Server- und Cron-Dienst) werden nun in einer einzigen `data/logs/application.log`-Datei konsolidiert.

3. **Protokollrotation**: Die Protokollrotation ist über die Umgebungsvariable `LOG_ROTATION_VERSIONS` konfigurierbar (Standard: 5 Versionen).

4. **Keine Datenbankmigrationen erforderlich**: Diese Version erfordert keine Änderungen am Datenbankschema.

---

## API-Endpunkte

Der folgende API-Endpunkt wurde in dieser Version eingeführt:

### Passwortrichtlinien-Endpunkt

- `GET /api/auth/password-policy` - Gibt die aktuelle Passwortrichtlinienkonfiguration zurück

### Anwendungsprotokolle-Endpunkte

- `GET /api/application-logs` - Zeigen Sie Protokolle mit Dateiauswahl und Tail-Funktionalität an
- `GET /api/application-logs/export` - Gefilterte Protokolle als Textdatei exportieren

### Entfernte Endpunkte

Die folgenden ungenutzten API-Endpunkte wurden in dieser Version entfernt:

- `DELETE /api/backups/[backupId]` - Nur für Entwicklung bestimmter Endpunkt, der nie verwendet wurde
- `GET /api/audit-log/stats` - Endpunkt wurde vom Audit-Log-Viewer nicht aufgerufen

---

## Unterstützung

### Hilfe erhalten

- **Dokumentation**: [Benutzerhandbuch](../user-guide/overview.md)
- **E-Mail-Einstellungen**: [E-Mail-Konfigurationsleitfaden](../user-guide/settings/email-settings.md)
- **API-Referenz**: [API-Dokumentation](../api-reference/overview.md)
- **Migrationsleitfaden**: [Versionsaktualisierungsmigration](../migration/version_upgrade.md)
- **Community**: [GitHub Discussions](https://github.com/wsj-br/duplistatus/discussions)
- **Probleme**: [GitHub Issues](https://github.com/wsj-br/duplistatus/issues)

### Fehler melden

Bitte geben Sie beim Melden von Fehlern Folgendes an:

- Version: 1.3.0
- Betriebssystem und Version
- Docker/podman-Version
- Container-Typ (Docker oder podman/Pod)
- Fehlermeldungen und Protokolle
- Schritte zum Reproduzieren

---

## Änderungsprotokoll

### Version 1.3.0 Änderungen

- **Hinzugefügt**: Konfigurierbare Passwortrichtlinie über `PWD_ENFORCE`- und `PWD_MIN_LEN`-Umgebungsvariablen
- **Hinzugefügt**: Anwendungsprotokoll-Viewer in Einstellungen (nur Admin)
- **Hinzugefügt**: Protokollrotation mit konfigurierbarer Versionszahl (`LOG_ROTATION_VERSIONS`)
- **Hinzugefügt**: Kontextabhängige Schaltfläche "Benutzerhandbuch", die relevante Dokumentation öffnet
- **Hinzugefügt**: Rechtsklick auf Serverkonfiguration zum Öffnen der Duplicati-Legacy-Benutzeroberfläche
- **Hinzugefügt**: API-Endpunkt `GET /api/auth/password-policy` für Passwortrichtlinienkonfiguration
- **Hinzugefügt**: API-Endpunkte zum Anzeigen und Exportieren von Anwendungsprotokollen
- **Geändert**: Die Wochentagsanzeige respektiert nun die Gebietsschemaeinstellungen des Browsers
- **Geändert**: Fehler-Toasts bleiben bestehen, bis sie manuell geschlossen werden
- **Geändert**: Erfolgs-Toasts werden nach 3 Sekunden automatisch geschlossen (zuvor 5 Sekunden)
- **Geändert**: Benachrichtigungseinstellungstabelle vereinfacht mit kombinierter Spalte "Server / Sicherung"
- **Geändert**: Alle Container-Protokolle in einer einzigen `application.log`-Datei konsolidiert
- **Verbessert**: SMTP-Fehlermeldungen bieten genauere Anleitung
- **Entfernt**: Ungenutzte API-Endpunkte (`DELETE /api/backups/[backupId]` und `GET /api/audit-log/stats`), die von der Anwendung nicht aufgerufen wurden

---

## Lizenz

Dieses Projekt ist unter der [Apache License 2.0](https://github.com/wsj-br/duplistatus/blob/main/LICENSE) lizenziert.

**Copyright © 2025 Waldemar Scudeller Jr.**
