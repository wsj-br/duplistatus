# समय क्षेत्र {/* #timezone */}

एप्लिकेशन उपयोगकर्ता इंटरफेस तिथि और समय को ब्राउज़र की सेटिंग्स के अनुसार प्रदर्शित किया जाएगा। लॉगिंग अभी भी `TZ` वातावरण चर का उपयोग करती है। दैनिक सारांश सूचनाएं [सेटिंग्स → दैनिक सारांश](../user-guide/settings/daily-summary-settings.md) में सहेजे गए आईएएनए समय क्षेत्र का उपयोग करती हैं, `TZ` नहीं। दैनिक सारांश के अलावा अन्य सूचना टाइमस्टैम्प अभी भी `TZ` का अनुसरण करते हैं।

यदि यह वातावरण चर सेट नहीं है तो डिफ़ॉल्ट मान `TZ=Europe/London` है।

:::note
सूचनाओं के लिए भाषा और स्थानीय सेटिंग्स (संख्या और तिथि प्रारूप) को [सेटिंग्स → टेम्पलेट](../user-guide/settings/notification-templates.md) में कॉन्फ़िगर किया जा सकता है।
:::

## समय क्षेत्र कॉन्फ़िगर करना {/* #configuring-the-timezone */}

एप्लिकेशन उपयोगकर्ता इंटरफेस तिथि और समय को ब्राउज़र की सेटिंग्स के अनुसार प्रदर्शित किया जाएगा। लॉगिंग अभी भी `TZ` वातावरण चर का उपयोग करती है। दैनिक सारांश सूचनाएं [सेटिंग्स → दैनिक सारांश](../user-guide/settings/daily-summary-settings.md) में सहेजे गए आईएएनए समय क्षेत्र का उपयोग करती हैं, `TZ` नहीं। दैनिक सारांश के अलावा अन्य सूचना टाइमस्टैम्प अभी भी `TZ` का अनुसरण करते हैं।

यदि यह वातावरण चर सेट नहीं है तो डिफ़ॉल्ट मान `TZ=Europe/London` है।

उदाहरण के लिए, समय क्षेत्र को साओ पाउलो में बदलने के लिए, निर्देशिका `duplistatus` में `compose.yml` में ये पंक्तियाँ जोड़ें:

```yaml
environment:
  - TZ=America/Sao_Paulo
```

या कमांड लाइन में (डॉकर या पॉडमैन) वातावरण चर पास करें:

```bash
  --env TZ=America/Sao_Paulo
```

### अपने लिनक्स कॉन्फ़िगरेशन का उपयोग करना {/* #using-your-linux-configuration */}

अपने लिनक्स होस्ट के कॉन्फ़िगरेशन को प्राप्त करने के लिए, आप निम्नलिखित निष्पादित कर सकते हैं:

```bash
echo TZ=\"$(</etc/timezone)\"
```

### समय क्षेत्रों की सूची {/* #list-of-timezones */}

आप समय क्षेत्रों की एक सूची यहां पा सकते हैं: [विकिपीडिया: टीजेड डेटाबेस समय क्षेत्रों की सूची](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List)
