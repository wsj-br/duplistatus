/** Must match `sourceLocale` in ai-i18n-tools.config.json */
export const SOURCE_LOCALE = "en-GB";

/** Cookie name for locale preference (same as root layout / proxy). */
export const LOCALE_COOKIE_NAME = "NEXT_LOCALE";
